# D1 Migrations

Phase 2A database migrations will live here.

Current config binds D1 as `DB` with database name `nut-house-portal-db`.
Replace the placeholder `database_id` in `wrangler.jsonc` after the real Cloudflare D1 database is confirmed or created.
